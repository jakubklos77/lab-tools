# Setup fabric to use the vendor and model:
f -S

# Edit the ~/app/fabric/config/.env file to manually
DEFAULT_VENDOR=Ollama
DEFAULT_MODEL=qwen2.5:1.5b

DEFAULT_VENDOR=OpenAI
DEFAULT_MODEL=gpt-4o-mini

GEMINI_API_KEY=
OPENAI_API_KEY=
